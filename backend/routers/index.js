const userRouter = require('./userRouters');
const productRouter = require('./productRouters');
const authRouter = require('./authRouter');



module.exports = {
    userRouter, productRouter, authRouter
}